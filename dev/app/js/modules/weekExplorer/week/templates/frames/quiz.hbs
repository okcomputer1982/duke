<h1>Quiz</h1>
<div class="inner-wrap">

<h2 class="subHeading">{{{content.title}}}</h2>

<div class="quiz"></div>
	{{#each content.questions}}
		{{setQuizIndex}}

		<div class="quizItem" data-index="{{getQuizIndex}}">
			<p>{{{this.title}}}</p>
			{{setResponseIndex}}
			{{#each this.responses}}
				<input type="radio" class="response" name="quiz{{getQuizIndex}}" data-index="{{getResponseIndex}}" value="this">{{{this}}}<br>
			{{/each}}
		</div>
		<hr/>
	{{/each}}

	<button type="submit" class="submit btn btn-large btn-primary attributeTarget eventTarget">Submit</button>
</div>